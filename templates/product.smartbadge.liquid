{% layout none %}{ "id": {{ product.id | json }}, "price": {{ bold_price | json }},{% if bold_compare_at_price %} "compare_at_price": {{ bold_compare_at_price | json }},{% endif %} "handle": {{ product.handle | json }},{% if product.tags %} "tags": {{ product.tags | json }},{% endif %} "published_at": "{{ product.published_at | date: "%s" }}", "variants": [ {% for variant in product.variants %}
{%- include 'bold-variant' with variant, hide_action: 'skip' -%} {"id": {{ variant.id }} {% if variant.inventory_management and variant.inventory_management != blank %}, "inventory_management": "{{ variant.inventory_management }}"{% endif %}{% if variant.inventory_quantity %}, "inventory_quantity": {{ variant.inventory_quantity }}{% endif %} } {% unless forloop.last %},{% endunless %} {% endfor %} ], "collections": [{% for collection in product.collections %}"{{ collection.id }}"{% unless forloop.last %},{% endunless %}{% endfor %}]}
