/* Product Tile */
.ProductItem__Label {
  font-size: 12px;
  text-transform: uppercase;
  font-weight: 700;
  color: #000;
}

.ProductItem__Label--onSale {
  color: #FF0055;
}

/* Header */
.Header:not(.Header--transparent) .HorizontalList__Item.is-expanded .Header__LinkSpacer::after, .Header:not(.Header--transparent) .HorizontalList__Item.is-active .Header__LinkSpacer::after {
  opacity: 0;
  transform: none;
}

/* MegaMenu */
.MegaMenu__Title {
	font-size: 12px;
  	font-weight: 700;
	color: #000;
}

/* Filters */
.CollectionFilters .ColorSwatch {
	border: 1px solid currentColor !important;
}


/* Buttons */
.Button {
  font-weight: 600;
}

/* Sizeswatch */
.SizeSwatch {
  min-height: 36px;
  font-size: 13px;
	font-weight: bold;
}

.SizeSwatch:hover {
	border: 1px solid #000;
}


/* AfterPay*/
.afterpay-link, .afterpay-text3 {
	display: none;
}

.ColorSwatch__Radio {
	border-radius: 25px;
}


/* Sale message */
.ProductMeta__SaleMessageContainer {
  font-family: var(--sale_message_font_family);
  font-weight: var(--sale_message_font_weight);
  font-style: var(--sale_message_font_style);
  font-size: var(--sale_message_font_size);
  color: var(--sale_message_color);
  margin: 24px 0 0;
}

/* Newsletter message */
.ImageHero__ContentOverlay {
    text-shadow: none;
}

.Header__MainNav .HorizontalList li:nth-child(7) > .Heading {
    color: #ff0055;
}


/* Cart Drawer */
.Cart__ShippingNotice {
    border-bottom: none;
}

.Drawer__Header--bordered {
    box-shadow: none;
}

.Cart .Drawer__Footer {
    padding: 0 30px 30px 30px;
}

.Drawer__Footer {
    box-shadow: none;
}

/* Slideshow */
.flickity-slider {
.SectionHeader__Heading--emphasize {
	margin: 0 16px 16px 0;
	text-shadow: 0 0 12px rgba(0, 0, 0, .5);
}
}


/* Collection Tile*/
.CollectionItem__Content--bottomCenter {
    bottom: 20px;
}

/* #section-collection-list .Button {
    font-size: 14px;
    font-size: .875rem;
    font-weight: 700;
    letter-spacing: .5px;
    letter-spacing: .035em;
    color: #000;
    text-transform: none;
    text-decoration: none;
    line-height: 1.9;
    padding: 0;
    background-color: none;
    margin: .8em 0;
    border: 0;
    border-bottom: 2px solid #000;
    height: auto;
    cursor: pointer;
    white-space: nowrap;
} */

.Header__CartDot {
    background-color: #7ED321;
}

i.icon-swym-wishlist {
  font-family: 'swym-icons';
  font-style: normal;
  vertical-align: middle;
}
i.icon-swym-wishlist+span {
  vertical-align: middle;
}
i.icon-swym-wishlist:after {
  content: '';
  font-size: 130%;
  line-height: 18px;
  text-align: center;
  display: inline;
}
/*content rendered after swym is ready to avoid showing block/invalid chars*/
.swym-ready i.icon-swym-wishlist:after {
  content: '\f004';
}

.swym-button-bar {
  .Product__Info & {
    display: none;
  }
}

.ProductForm__AddToCartContainer {
  display: flex;
  align-items: center;
  .ProductForm__AddToCart {
    flex: 1;
  }
}

.custom-wishlist-button.swym-add-to-wishlist-view-product {
  padding: 10px !important;
  border: 1px solid #000 !important;
  font-size: 1.6em !important;
  margin-right: 10px !important;
  &:after {
    color: #000 !important;
  }
  &.swym-added{
    opacity: 1 !important;
    &:after {
      color: #000 !important;
    }
  }
}
